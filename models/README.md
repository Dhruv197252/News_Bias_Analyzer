This directory holds trained ML model artifacts.

bias_classifier.pkl      — TF-IDF + Logistic Regression (v1, production)
bias_classifier_v2.pkl   — TF-IDF + XGBoost (v2, if trained)

To regenerate: python training/train_model.py
